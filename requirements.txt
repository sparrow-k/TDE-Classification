# Versions used during development (Python 3.14, Windows, CUDA 13.2). A GPU is optional; everything runs on CPU.
# PyTorch with CUDA: install from https://download.pytorch.org/whl/cu132
torch==2.12.0
numpy==2.4.6
pandas==2.3.3
scikit-learn==1.8.0
pyarrow==23.0.1
PyYAML==6.0.3
matplotlib==3.10.8   # live demo and figures

# Only needed to rebuild the presentation (PRESENTATION DEMO/build/build_presentation.py)
python-pptx==1.0.2
